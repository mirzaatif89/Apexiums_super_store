# This site is served by the cPanel Node.js application (app.js).
# Do not add Apache DirectoryIndex or rewrite rules here: they bypass the
# Node application and cause a Not Found response when Vite output is ignored.
